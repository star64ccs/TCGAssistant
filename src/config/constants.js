// 應用程式常數配置 - 已遷移到統一配置管理系統
// 請使用 src/config/unifiedConfig.js 中的配置

import {
  STORAGE_KEYS,
  APP_CONFIG,
  MEMBERSHIP_TYPES,
  API_ENDPOINTS,
} from './unifiedConfig';

// 向後兼容的常數導出
export { STORAGE_KEYS, APP_CONFIG, MEMBERSHIP_TYPES, API_ENDPOINTS };
